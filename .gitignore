# Local Claude settings (contains personal paths)
.claude/settings.local.json

# Claude skills are symlinks to .agents/skills — only keep the source
.claude/skills/

# Video frames (large binary files, regenerated by FFmpeg)
nova-pro/frames/

# Temp install scripts
install_gh.ps1
install_gh2.ps1
